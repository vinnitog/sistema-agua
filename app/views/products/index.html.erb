<div class="container">
  <h1><span class="glyphicon glyphicon-list-alt"> Produtos </span></h1>
  <table class="table">
    <tr>
      <th>Nome</th>
      <th>Quantidade</th>
      <th>Preço</th>
      <th>Marca</th>
      <th>Visualizar</th>
      <th>Editar</th>
      <th>Deletar</th>
    </tr>
   
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %></td>
        <td><%= product.quantity %></td>
        <td><%= product.price %></td>
        <td><%= product.brand %></td>
        <td><%= link_to 'Visualizar', product_path(product), class: 'btn btn-default' %></td>
        <td><%= link_to 'Editar', edit_product_path(product), class: 'btn btn-default' %></td>
        <td><%= link_to 'Deletar', product_path(product),
                method: :delete,
                data: { confirm: 'Tem certeza que quer deletar este produto?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </table><br/>
  <%= link_to 'Cadastrar Novo Produto', new_product_path, class: 'btn btn-primary' %>
</div>